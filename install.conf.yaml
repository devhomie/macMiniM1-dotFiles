- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.zshrc: zshrc
#     ~/.tmux.conf: tmux.conf
#     ~/.vim: vim
#     ~/.vimrc: vimrc

- create:
    - ~/Work
    

# - shell:
#  - [git submodule update --init --recursive, Installing submodules] 
