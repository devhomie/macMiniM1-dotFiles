- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.zshrc: 
    ~/.gitconfig:
#     ~/.tmux.conf: tmux.conf
#     ~/.vim: vim
#     ~/.vimrc: vimrc

- create:
    - ~/Work
    

- shell:
   - 
      command: ./setup_homebrew.zsh
      stdout: true
      stderr: true
